feedparser>=6.0.11
requests>=2.31.0
python-dateutil>=2.9.0
anthropic>=0.49.0
# Mimi Helen Bot uses stdlib zoneinfo; tzdata guarantees the tz database
# is present across platforms (notably slim/Windows images).
tzdata>=2024.1
